import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "in.maniware.drawingBoard",
  appName: "Canvas by ManiWare",
  webDir: "dist",
};

export default config;
